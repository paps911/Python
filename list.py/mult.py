#zeros = [0]*50
#final_list = list1 + list2
#Slicing a list
numbers = [7,4,2,3]
    #numbers[2:]
#max(numbers)
#min(numbers)

#Multiplying a list

#get the minimum value
    #min(numbers)

#get the maximum value
    #max(number)

print(max(numbers))